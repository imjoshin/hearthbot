version: '3'
services:
  api:
    image: hearthbot-api
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"

  web-cli:
    image: hearthbot-api
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge
